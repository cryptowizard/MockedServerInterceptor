package world.skytale.mocks.interceptor.data

/**
 * This map contains mocked response codes for specific requests.
 * It can be used either at runtime or to add default response code for specific request.
 */
val mockedResponseCodeMap = mutableMapOf<String, Int>(
    "" to 200,
)